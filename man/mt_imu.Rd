% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/imu_col_attr.R
\name{mt_imu}
\alias{mt_imu}
\alias{mt_acc}
\alias{mt_mag}
\alias{mt_gyro}
\title{Access parsed IMU data stored in a \code{move2}}
\usage{
mt_acc(x)

mt_mag(x)

mt_gyro(x)
}
\arguments{
\item{x}{A \code{move2} object with an IMU vector column (for instance, produced
by \code{\link[=as_acc]{as_acc()}} or \code{\link[=as_mag]{as_mag()}}).}
}
\value{
An object inheriting from class \code{imu} containing the data in the indicated
column of \code{x}.
}
\description{
Return the values contained in the currently designated column storing
data for the indicated sensor.

To set a column that contains data from a given sensor, use the corresponding
setting function. For instance, \code{\link[=mt_set_acc_column]{mt_set_acc_column()}}.
}
\details{
The data contained in the designated IMU column for the given sensor must
be in the corresponding class. For instance, the \code{acc_column} set by
\code{\link[=mt_set_acc_column]{mt_set_acc_column()}} must contain an \code{acc} vector. \code{mt_acc()} validates
the data class before returning.
}
\examples{
\dontshow{if (rlang::is_installed("move2")) withAutoprint(\{ # examplesIf}
alb <- albatrosses()
alb$acc <- as_acc(alb)
alb <- mt_set_acc_column(alb, "acc")

mt_acc(alb)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=mt_acc_column]{mt_acc_column()}}, \code{\link[=mt_mag_column]{mt_mag_column()}}, \code{\link[=mt_gyro_column]{mt_gyro_column()}} to get or
set the column that contains parsed data for a given sensor.
}
