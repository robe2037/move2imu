% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/funs.R
\name{imu-fields}
\alias{imu-fields}
\alias{bursts}
\alias{bursts<-}
\alias{freqs}
\alias{freqs<-}
\alias{starts}
\alias{starts<-}
\title{Access and modify fields of an IMU vector}
\usage{
bursts(x)

bursts(x) <- value

freqs(x)

freqs(x) <- value

starts(x)

starts(x) <- value
}
\arguments{
\item{x}{An IMU vector (\code{acc}, \code{mag}, or \code{gyro})}

\item{value}{Replacement value.}
}
\value{
For accessors, the corresponding field of \code{x}. For setters, \code{x}
with the updated value in the indicated field.
}
\description{
Access or update the underlying burst matrices, sampling frequencies, or
start times for each burst in an IMU vector.
}
\details{
Frequencies assigned with \verb{freqs<-} are converted to Hz if they are provided
with compatible units attached. If no units are provided, the values are
assumed to be in Hz already.

Start times assigned with \verb{starts<-} accept \code{POSIXct}, \code{POSIXlt}, \code{Date},
or a number of seconds since \verb{1970-01-01 00:00:00 UTC}. Start times are
converted and stored as \code{POSIXct}. \code{Date} objects are treated as being
recorded at midnight, UTC.
}
\examples{
x <- acc(
  bursts = list(
    cbind(X = sin(1:20 / 10), Y = cos(1:20 / 10), Z = 1)
  ),
  frequency = units::as_units(20, "Hz"),
  start = as.POSIXct("2020-01-01 00:00:00", tz = "UTC")
)

bursts(x)

freqs(x)

starts(x)

freqs(x) <- units::as_units(25, "Hz")
freqs(x)
}
