% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/colsets.R
\name{movebank_colsets}
\alias{movebank_colsets}
\alias{movebank_acc_colsets}
\alias{movebank_mag_colsets}
\alias{movebank_gyro_colsets}
\title{View standard Movebank IMU data column sets}
\usage{
movebank_acc_colsets()

movebank_mag_colsets()

movebank_gyro_colsets()
}
\value{
A named list of \code{imu_colset} objects.
}
\description{
Movebank has several standard ways to store data for each IMU sensor. These
functions show the recognized columns for each sensor that can be extracted
from a \code{move2} or \code{data.frame} by default.
\itemize{
\item \code{movebank_acc_colsets()} — standard column sets for \code{\link[=as_acc]{as_acc()}}.
\item \code{movebank_mag_colsets()} — standard column sets for \code{\link[=as_mag]{as_mag()}}.
\item \code{movebank_gyro_colsets()} — standard column sets for \code{\link[=as_gyro]{as_gyro()}}.
}

To extract IMU data from columns whose names don't correspond to
Movebank's conventions, provide a custom set of IMU columns with
\code{\link[=imu_colset]{imu_colset()}}.
}
\details{
IMU data are stored in two ways:
\itemize{
\item \strong{Expanded-format} columns store each IMU sample (possibly for multiple axes)
in its own row.
\item \strong{Compact-format} columns store a burst of IMU samples as a space-delimited
string. This string must be segmented into axis-specific values using
an associated column that indicates the axes present in the burst.
A further column provides the sampling frequency of the burst. All three
of these columns must be present to form a valid compact-format column set.
}
\subsection{Alternate column name separators}{

Some column names may differ depending on how the data were downloaded.
The Movebank API (e.g. \code{move2::movebank_download_study()}) provides columns
with \verb{_} separators, while manually downloaded data uses \code{:} and \code{-}
separators and occasionally includes additional prefixes. For full
compatibility, the \verb{active_*_colsets()} functions recognize these alternate
spellings as additional column sets even though \verb{movebank_*_colsets()} lists
only the standard API names.

For future compatibility, consider converting data with
the manually-downloaded column names to use \verb{_} separators. To use
a custom column set, provide the names explicitly with
\code{\link[=imu_colset]{imu_colset()}}.
}
}
\examples{
movebank_acc_colsets()

movebank_mag_colsets()

movebank_gyro_colsets()
}
\seealso{
\code{\link[=active_acc_colsets]{active_acc_colsets()}}, \code{\link[=active_mag_colsets]{active_mag_colsets()}}, \code{\link[=active_gyro_colsets]{active_gyro_colsets()}}
to identify column sets present in a tabular data source.
}
