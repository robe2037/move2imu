% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/imu_col_attr.R
\name{mt_imu_column}
\alias{mt_imu_column}
\alias{mt_acc_column}
\alias{mt_mag_column}
\alias{mt_gyro_column}
\alias{mt_set_acc_column}
\alias{mt_set_mag_column}
\alias{mt_set_gyro_column}
\title{Get or set names of columns that contain IMU data in a \code{move2}}
\usage{
mt_acc_column(x)

mt_mag_column(x)

mt_gyro_column(x)

mt_set_acc_column(x, value)

mt_set_mag_column(x, value)

mt_set_gyro_column(x, value)
}
\arguments{
\item{x}{A \code{move2} object with an IMU vector column (for instance, produced
by \code{\link[=as_acc]{as_acc()}} or \code{\link[=as_mag]{as_mag()}}).}

\item{value}{A string giving the name of the column in \code{x} that contains
the extracted IMU data. The column must contain a vector of the
corresponding sensor type (e.g. \code{acc}).}
}
\value{
\verb{mt_*_column()} returns a length-one character vector naming the designated
column.

\verb{mt_set_*_column()} returns \code{x} with the corresponding attribute set to
\code{value}
}
\description{
Designate columns in a \code{move2} object that contain a given class of IMU
data. This allows you to flexibly store IMU data produced by \code{as_acc()},
\code{as_mag()}, or \code{as_gyro()} in a column in a \code{move2} and access it
programmatically, without needing to hard-code column names.
\itemize{
\item \verb{mt_*_column()} returns the name of the column in the input \code{move2} that
contains data from the indicated sensor.
\item \verb{mt_set_*_column()} sets the column that contains data from the indicated
sensor.
}
}
\details{
These functions behave similarly to move2's native helpers (e.g.
\code{\link[move2:column_name]{move2::mt_time_column()}}). They work by storing a character attribute
in the input \code{move2} object naming the relevant column of IMU data.

Be careful when modifying these columns with other methods. Operations
that rename a column without updating the attribute (e.g.
\code{\link[dplyr:rename]{dplyr::rename()}}) can create a mismatch between the stored column name
and the name of the column in the data themselves. The column name will
then need to be updated accordingly using \verb{mt_set_*_column()}.

Further, some operations may drop these attributes, including
\code{\link[dplyr:distinct]{dplyr::distinct()}}, \code{\link[dplyr:summarise]{dplyr::summarize()}}, and \code{\link[move2:mt_stack]{move2::mt_stack()}}.
}
\examples{
\dontshow{if (rlang::is_installed("move2")) withAutoprint(\{ # examplesIf}
alb <- albatrosses()
alb$acc <- as_acc(alb)
alb <- mt_set_acc_column(alb, "acc")

mt_acc_column(alb)

mt_acc(alb)[1:10]
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=mt_acc]{mt_acc()}}, \code{\link[=mt_mag]{mt_mag()}}, \code{\link[=mt_gyro]{mt_gyro()}} to extract the column values.

\code{\link[=as_acc]{as_acc()}}, \code{\link[=as_mag]{as_mag()}}, \code{\link[=as_gyro]{as_gyro()}} to build IMU columns.
}
