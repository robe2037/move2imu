% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/acc.R, R/mag.R, R/gyro.R
\name{acc}
\alias{acc}
\alias{mag}
\alias{gyro}
\alias{imu_constructors}
\title{Create an IMU vector}
\usage{
acc(
  bursts = list(),
  frequency = units::set_units(double(), "Hz"),
  start = NULL
)

mag(
  bursts = list(),
  frequency = units::set_units(double(), "Hz"),
  start = NULL
)

gyro(
  bursts = list(),
  frequency = units::set_units(double(), "Hz"),
  start = NULL
)
}
\arguments{
\item{bursts}{A list of matrices, one per burst. Each matrix has one
row per sample and named columns drawn from \code{"X"}, \code{"Y"}, and
\code{"Z"} indicating which axes were recorded. Entries can be \code{NULL}
to represent missing bursts.}

\item{frequency}{Sampling frequency of the recordings in \code{bursts},
either length 1 (recycled to all bursts) or the same length as
\code{bursts}. Frequencies with compatible \link[units:units]{units} are converted
to Hz internally. If no units are specified, the frequency is assumed to
be in Hz.}

\item{start}{Optional; burst start times. Either length 1 (recycled), the
same length as \code{bursts}, or \code{NULL} if start times are unknown.

Accepts \code{POSIXct}, \code{POSIXlt}, \code{Date}, or numeric values. \code{Date} objects
are treated as being recorded at midnight, UTC. Numeric values are
interpreted as the number of seconds since \verb{1970-01-01 00:00:00 UTC}.
Inputs are all converted to \code{POSIXct}. For inputs with a specified time
zone, it is retained.}
}
\value{
An IMU vector of the corresponding sensor type (\code{acc},
\code{mag}, or \code{gyro}).
}
\description{
Construct an \code{acc}, \code{mag}, or \code{gyro} vector from a list of burst
matrices and the sampling frequency at which they were recorded.

The three constructors share the same signature and semantics —
they differ only in the sensor type recorded by the resulting
vector and in the units typically attached to its samples
(acceleration, magnetic field strength, angular velocity).
}
\examples{
# Accelerometer: a single burst on X, Y, Z at 40 Hz
acc(
  bursts = list(cbind(
    X = sin(1:30 / 10),
    Y = cos(1:30 / 10),
    Z = 1:30 / 10
  )),
  frequency = units::as_units(40, "Hz")
)

# Multiple bursts with different axes — combined into one vector
acc(
  bursts = list(
    cbind(X = sin(1:20 / 10), Y = cos(1:20 / 10)),
    cbind(X = sin(1:20 / 10 + 2), Y = cos(1:20 / 10 + 3))
  ),
  frequency = units::as_units(30, "Hz")
)

# The same API for magnetometer and gyroscope data:
mag(
  bursts = list(cbind(X = 1:10, Y = 11:20, Z = 21:30)),
  frequency = units::as_units(10, "Hz")
)

gyro(
  bursts = list(cbind(X = sin(1:20 / 10), Y = cos(1:20 / 10))),
  frequency = units::as_units(50, "Hz")
)
}
