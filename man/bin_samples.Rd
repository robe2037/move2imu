% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_sampling_effort.R
\name{bin_samples}
\alias{bin_samples}
\title{Count samples within time bins for a set of sensors}
\usage{
bin_samples(..., ids = NULL, bin_width = NULL, from = NULL, to = NULL)
}
\arguments{
\item{...}{Any number of \code{imu} or timestamp vectors. All vectors must be the
same length. Timestamps can be in \code{POSIXct}, \code{POSIXlt}, \code{Date}, or a
number of seconds since \verb{1970-01-01 00:00:00 UTC}. \code{Date} objects are
treated as being recorded at midnight, UTC.}

\item{ids}{Vector of IDs used to group the observations in \code{...}. All
observations for each group will be included in a single panel. Must be
the same length as each of the vectors in \code{...}.}

\item{bin_width}{Width of the time bins within which samples are counted.
Provided as a \link[units:units]{units} object, a \link{difftime} object, or a
numeric value which will be interpreted as seconds. By default, the time
range of the plot is divided into roughly 300 bins.

Decrease the \code{bin_width} to increase plot resolution, at the expense of
legibility for sparsely collected data.}

\item{from, to}{Start and end timestamps defining the range within which
samples will be counted. Accepts the same formats as timestamps in \code{...}.
By default, the full temporal extent of the data is used.}
}
\value{
A data frame with one row per non-empty lane, id, and bin, containing
the following columns:
\itemize{
\item \code{lane}: The source vector in \code{...}, labeled with that
argument's name or expression. Each lane is drawn as one
row by \code{\link[=plot_sampling_effort]{plot_sampling_effort()}}.
\item \code{id}: Grouping identifier, as a factor. Absent when \code{ids} is \code{NULL}.
\item \code{time}: Bin start, in the timezone of the first lane.
\item \code{n}: Samples recorded in the bin.
\item \code{rate}: Effective sampling rate, \code{n / bin_width}, in Hz.
\item \code{effort}: \code{rate} as a fraction of the highest rate in the same lane,
taken across all values of \code{ids}.
}

The bin width in seconds is attached as the \code{"bin_width"} attribute.
}
\description{
Summarize sensor sampling regimes for a set of \code{imu} or timestamp vectors by
counting samples within fixed time bins. This produces the data that is
used by \code{\link[=plot_sampling_effort]{plot_sampling_effort()}}, exposed for custom plots.
}
\details{
Sampling effort is calculated by
\enumerate{
\item splitting the plotted time range into equal-width bins. The binned grid
starts at \code{from} if provided. Otherwise, it starts at the closest multiple
of \code{bin_width} at or before the first sample.
\item counting the number of samples each sensor recorded in each bin, separately
for every track.
\item dividing each count by the bin width, giving an effective sampling rate
in Hz.
\item normalizing those rates within each lane, so that each lane's sampling
effort values are relative to the maximum sampling effort in that lane,
across all values of \code{ids}.
}
}
\examples{
\dontshow{if (rlang::is_installed(c("move2", "sf"))) withAutoprint(\{ # examplesIf}
alb <- albatrosses()

bin_samples(acc = as_acc(alb), ids = move2::mt_track_id(alb))
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=plot_sampling_effort]{plot_sampling_effort()}} to plot the output of \code{bin_samples()}
}
\keyword{internal}
